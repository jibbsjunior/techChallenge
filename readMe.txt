clone this folder into your local computer
do npm install to install all the dependencies
navigate to the folder after that in your terminal and do npm start or yarn start
goto your browser and type localhost:3000 if the dev server did not start automatically

start the back server too in order for the app so fetch data for the UI